apiVersion: 1

providers:
  - name: 'NeuroDispatch Dashboards'
    orgId: 1
    folder: 'NeuroDispatch'
    folderUid: 'neuro_dispatch'
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards

